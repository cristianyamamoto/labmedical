import { BirthdateToAgePipe } from './birthdate-to-age.pipe';

describe('BirthdateToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new BirthdateToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
